package com.awardhub.profile.entity;

/**
 * Supporting document types a nominee can attach to their profile, per the
 * examples in the requirement-gathering report (NIC/passport copy, CV,
 * certificates, evidence files).
 */
public enum NomineeDocumentType {
    NIC_PASSPORT_COPY,
    CV,
    CERTIFICATE,
    ACHIEVEMENT_PROOF,
    PROFILE_PHOTO,
    OTHER
}
