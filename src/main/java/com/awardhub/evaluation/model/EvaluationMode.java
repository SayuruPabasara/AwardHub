package com.awardhub.evaluation.model;

/** How a category decides its winner. Set by the Award Organizer before the cycle opens. */
public enum EvaluationMode {
    JUDGE_ONLY,
    PUBLIC_ONLY,
    HYBRID
}
